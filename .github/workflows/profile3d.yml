# Superseded by generate-assets.yml — kept to avoid broken workflow references.
# Do not enable triggers here.
name: Generate 3D Contribution Calendar (disabled)
on:
  workflow_dispatch:
    inputs:
      note:
        description: "Use generate-assets.yml instead"
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the 'Generate Profile Assets' workflow instead."
